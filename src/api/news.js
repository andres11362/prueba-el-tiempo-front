/**
 * Configuracion de las rutas para noticias se usa el api key 
* de la carpeta constants
 */
import { apiKey } from '../constants/api';

// eslint-disable-next-line import/prefer-default-export
export const news = `${apiKey}/noticias`;
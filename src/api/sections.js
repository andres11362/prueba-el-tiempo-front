import { apiKey } from '../constants/api';

// eslint-disable-next-line import/prefer-default-export
export const sections = `${apiKey}/secciones`;
export const sectionsSelect = `${apiKey}/secciones-select`;